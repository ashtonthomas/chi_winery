module ComponentsApi
  module SpecBuilder
    # Can we dynamically create specs for the producing API?
    # this might be handy to make sure our mocks are always uptoday

    #case: we change our producing api, we want to make sure
    #we don't forget to update the pacts defined here
  end
end
